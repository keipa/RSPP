<%= form_for [ commentable, commentable.comments.build ], remote: true do |f| %>
  <%= f.text_area :comment, class: "form-input",
    placeholder: "Добавить комментарий..." %>
  <div class="actions">
    <%= f.submit "Отправить", class: "create-btn", id: "send-comment" %>
  </div>
<% end %>
