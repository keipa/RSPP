$(".for-" + "<%= @commentable.class.to_s.downcase %>" + "-" + "<%= @commentable.id %>")
  .html("<%= escape_javascript render (@commentable.comments.order(created_at: :desc)) %>");
console.log("comment added")
