<%= javascript_tag do %>
window.currentUserId = '<%= current_user.id %>'
<% end %>
<div class="survey-title">
    <%= @survey.title %>
</div>
<div class="survey-content" data-controller="<%= survey_path(@survey) %>">

</div>
