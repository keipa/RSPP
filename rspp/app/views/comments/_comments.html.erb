<div class="comments-content-<%= commentable.id%>" data-id="<%= commentable.id%>">
    <div class="panel panel-default widget comments-body">
        <div class="panel-heading">
            <span class="glyphicon glyphicon-comment"></span>
            <h3 class="panel-title">
                Последние комментарии
            </h3>

            <span class="label label-info comment-size">
                <%= commentable.comments.size %>
            </span>
        </div>
        <a class="btn btn-primary btn-sm btn-block show-comments" role="button">
            <span class="glyphicon glyphicon-chevron-down"></span>
            Показать комментарии
        </a>
        <div class="panel-body">
            <ul class="list-group comments-list">
                <% commentable.comments.each do |comment| %>
                <%= render comment, commentable: commentable %>
                <% end %>
            </ul>
        </div>
    </div>
</div>
